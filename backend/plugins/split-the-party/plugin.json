{
  "id": "split-the-party",
  "name": "Split the Party",
  "version": "1.0.0",
  "description": "Route different players onto different maps in the same session. Each player's view follows their assignment regardless of which map the DM is currently looking at — useful for splitting an exploration group from a combat group, staging incoming players in a lobby map, or running parallel scenes. DM-only assignment UI sits in the Session tab.",
  "author": "TableTop Forge",
  "requires": [],
  "frontend_entry": "client.js",
  "extension_points": ["panel_tab_extension", "player_map_override"]
}
