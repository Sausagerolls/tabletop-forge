{
  "name": "tabletopforge-native",
  "version": "1.9.13",
  "private": true,
  "description": "Tauri-shelled native build of TableTop Forge — wraps the Express + PGlite backend in a webview-only desktop app.",
  "scripts": {
    "build:frontend": "npm --prefix frontend run build",
    "tauri": "tauri",
    "tauri:dev":   "npm run build:frontend && tauri dev",
    "tauri:build": "npm run build:frontend && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
